//Q17) Write a program to find maximum between two numbers
var numOne=Number(window.prompt("enter number one "));
var numTwo=Number(window.prompt("enter number Two "));
var numMax;
switch(numMax){
    case numMax:
        if(numOne>numTwo){
        document.write("number one is the max : " + numOne);
        }
        else{
            document.write("number two is max : " + numTwo);
        }
        break;
}
